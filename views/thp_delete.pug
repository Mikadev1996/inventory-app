extends layout

block content
    nav
        div.nav-content
            div.nav-text
                p BAT Portfolio
        div.nav-links
            a(href='/')
                p Home
            a(href='/category/thp')
                p Back to THPs

    main.content
        div.content-display
            h1.content-heading Are you sure you want to delete this device?
            div.content-display
                form(method='POST' action='')
                    .form-control
                        label(for='delete_thp')
                        input#delete_thp(type='hidden', name='delete_thp', required='true', value=thp_id)
                    div
                        button.submit-delete(type='submit') Submit
    footer
        div.footer-content
            p Copyright © 2022, GitHub // Mika.dev
        div.footer-border
